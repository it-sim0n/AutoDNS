[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "autodns"
version = "1.0.0"
description = "Portable DNS automation wrapper for puredns"
requires-python = ">=3.8"

[project.scripts]
autodns = "autodns.main:main"
